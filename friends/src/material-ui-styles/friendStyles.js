import { makeStyles } from '@material-ui/core/styles';

export const friendStyles = makeStyles(theme => ({

    item: {
        borderTop: 1,
        borderBottom: 1, 
        borderColor: 'black',
    },

}));